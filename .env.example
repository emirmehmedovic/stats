# Database Configuration with Connection Pooling
# For optimal performance, use connection pooling

# Standard PostgreSQL connection (without pooling)
# DATABASE_URL="postgresql://user:password@localhost:5432/stats_db"

# PostgreSQL with connection pooling parameters (RECOMMENDED)
# Adjust pool size based on your needs:
# - For development: connection_limit=10
# - For production with moderate traffic: connection_limit=20-50
# - For high traffic: connection_limit=100+
# connect_timeout is in seconds, statement_timeout in milliseconds
DATABASE_URL="postgresql://user:password@localhost:5432/stats_db?connection_limit=30&pool_timeout=20&connect_timeout=10&statement_timeout=60000"

# Alternative: Using Prisma Accelerate or PgBouncer (BEST for production)
# DATABASE_URL="postgresql://user:password@pooler.example.com:5432/stats_db"

# Next.js
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Authentication (if applicable)
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# Billing/Finance Security
# PIN for accessing financial reports (Naplate section)
# IMPORTANT: This should be a 4-6 digit numeric PIN
# Example: 1234 or 123456
BILLING_PIN=your-4-6-digit-pin-here
